# ============================================================================
# 医疗多智能体系统 - Docker环境变量配置模板
# 使用说明：
#   1. 复制此文件为 .env: cp env.example .env
#   2. 根据实际情况修改配置值
#   3. 不要将 .env 文件提交到版本控制系统
# ============================================================================

# ----------------------------------------------------------------------------
# Neo4j 数据库配置
# ----------------------------------------------------------------------------
# 完整部署时：使用容器内的服务名
# 应用部署时：使用外部数据库的实际地址
NEO4J_URI=bolt://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=test1234

# Neo4j容器配置（仅完整部署时生效）
NEO4J_MEMORY_PAGECACHE=1G
NEO4J_MEMORY_HEAP_INITIAL=1G
NEO4J_MEMORY_HEAP_MAX=2G

# ----------------------------------------------------------------------------
# Redis 配置
# ----------------------------------------------------------------------------
# 完整部署时：使用容器内的服务名
# 应用部署时：使用外部数据库的实际地址（如 host.docker.internal 或 IP地址）
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ----------------------------------------------------------------------------
# LLM 配置（大语言模型）
# ----------------------------------------------------------------------------
LLM_MODEL=qwen2.5:14b
LLM_BASE_URL=https://zjlchat.vip.cpolar.cn/v1
LLM_API_KEY=EMPTY
LLM_TEMPERATURE=0.1
LLM_TOP_P=0.8

# ----------------------------------------------------------------------------
# MCP 服务配置
# ----------------------------------------------------------------------------
# 在Docker内部，使用容器服务名
MCP_TRIAGE_URL=http://mcp:8000/sse

# ----------------------------------------------------------------------------
# 文档处理配置
# ----------------------------------------------------------------------------
CHUNK_SIZE=2000
CHUNK_OVERLAP=200
REQUEST_INTERVAL=1

# ----------------------------------------------------------------------------
# 服务端口配置
# ----------------------------------------------------------------------------
# 后端API端口（宿主机端口映射）
BACKEND_PORT=8012

# MCP服务端口（宿主机端口映射）
MCP_PORT=8000

# 前端Web端口（宿主机端口映射）
FRONTEND_PORT=80

# Neo4j端口（仅完整部署时生效，宿主机端口映射）
NEO4J_HTTP_PORT=7474
NEO4J_BOLT_PORT=7687

# Redis端口（仅完整部署时生效，宿主机端口映射）
REDIS_EXTERNAL_PORT=6379

# ----------------------------------------------------------------------------
# 可选配置
# ----------------------------------------------------------------------------
# 宿主机模型路径（如果已下载m3e-base模型，可指定路径以避免重复下载）
# HOST_MODEL_PATH=./RAG/models

